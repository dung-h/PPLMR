This directory vendors the TyC compiler frontend used by the PPL scope/binding demo.

- `build/`: ANTLR-generated lexer/parser/visitor (TyC).
- `src/`: TyC AST nodes, AST generation, and static checker.

This is copied from `Assignment1_PPL/tyc-compiler` so the PPLMR repo can run
independently.
